---
plan_id: "03-01"
phase: 03-dynamic-dns-and-audit
name: Multi-source IP resolver and DuckDNS integration
wave: 1
autonomous: true
objective: >
  Implement a hardened public IP resolver that queries 3 independent HTTPS sources
  and requires 2-of-3 consensus, plus a DuckDNS updater that reads the token from
  the vault (never plaintext), uses HTTPS with cert verification, validates the
  "OK" response, and logs every attempt to the audit log.
files_modified:
  - src/wg_automate/dns/__init__.py
  - src/wg_automate/dns/ip_resolver.py
  - src/wg_automate/dns/duckdns.py
task_count: 2
requirement_ids: [DNS-01, DNS-02, DNS-03, DNS-04, DNS-05]
depends_on: []

must_haves:
  truths:
    - "Public IP is resolved only when 2 of 3 HTTPS sources agree on the same address"
    - "If fewer than 2 sources agree, resolve_public_ip() raises IPConsensusError (fail closed)"
    - "Resolved IP is confirmed as public IPv4 (private, multicast, loopback rejected)"
    - "DuckDNS token is loaded from the vault as SecretBytes and never logged or passed as a CLI argument"
    - "DuckDNS update uses HTTPS with verify=True; response body must be exactly 'OK'"
    - "Every DuckDNS update attempt is passed to the audit log by the caller (update_dns returns a result dict)"
    - "update_dns() is the function the platform scheduler calls -- it is self-contained and importable without side effects"
  artifacts:
    - path: "src/wg_automate/dns/__init__.py"
      provides: "Public API exports for ip_resolver and duckdns"
    - path: "src/wg_automate/dns/ip_resolver.py"
      provides: "resolve_public_ip() -> IPv4Address, IPConsensusError, _IP_SOURCES constant"
      exports: [resolve_public_ip, IPConsensusError]
    - path: "src/wg_automate/dns/duckdns.py"
      provides: "update_dns(domain, token: SecretBytes) -> dict, DuckDNSError"
      exports: [update_dns, DuckDNSError]
  key_links:
    - from: "src/wg_automate/dns/ip_resolver.py"
      to: "ipify, amazonaws, icanhazip HTTPS endpoints"
      via: "urllib.request.urlopen with ssl.create_default_context()"
      pattern: "ssl\\.create_default_context"
    - from: "src/wg_automate/dns/duckdns.py"
      to: "https://www.duckdns.org/update"
      via: "urllib.request with HTTPS + cert verification"
      pattern: "duckdns\\.org/update"
    - from: "src/wg_automate/dns/duckdns.py"
      to: "security/vault.py"
      via: "token parameter typed as SecretBytes"
      pattern: "SecretBytes"
---

<objective>
Build the Dynamic DNS foundation: a consensus-based public IP resolver and a
hardened DuckDNS updater that sources its token exclusively from the vault.

Purpose: DuckDNS updates require a verified public IP and a never-plaintext
token. Both invariants must be enforced structurally (types + fail-closed logic),
not by convention.

Output:
- src/wg_automate/dns/ip_resolver.py — 3-source HTTPS resolver with 2-of-3 consensus
- src/wg_automate/dns/duckdns.py — DuckDNS HTTPS updater with SecretBytes token
- src/wg_automate/dns/__init__.py — package init with public API
</objective>

<execution_context>
@C:/Users/mudig/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mudig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/ClaudeCode/VirtualFTP/.planning/PROJECT.md
@E:/ClaudeCode/VirtualFTP/.planning/ROADMAP.md
@E:/ClaudeCode/VirtualFTP/.planning/STATE.md

@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/secret_types.py
@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/vault.py
@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/exceptions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-source public IP resolver with 2-of-3 consensus</name>
  <files>
    src/wg_automate/dns/__init__.py
    src/wg_automate/dns/ip_resolver.py
  </files>
  <action>
Create src/wg_automate/dns/__init__.py as a minimal package init — export
resolve_public_ip, IPConsensusError, update_dns, DuckDNSError from their
respective submodules. Keep imports lazy or guard with TYPE_CHECKING to avoid
import errors before duckdns.py exists.

Create src/wg_automate/dns/ip_resolver.py:

1. Define _IP_SOURCES as a module-level tuple of 3 HTTPS URL strings:
   - "https://api.ipify.org"
   - "https://checkip.amazonaws.com"
   - "https://icanhazip.com"
   Use exactly these URLs (no HTTP alternatives, no fallbacks to HTTP).

2. Define IPConsensusError(Exception) — raised when fewer than 2 sources agree.

3. Implement _fetch_ip(url: str, timeout: float = 5.0) -> str | None:
   - Use urllib.request.urlopen with an ssl.create_default_context() to ensure
     certificate verification is active (verify=True equivalent for urllib).
   - Strip whitespace from response body.
   - Return None on any exception (network error, timeout, bad response) — never
     propagate individual source failures.
   - Do NOT use requests library — stdlib urllib only (no new dependencies).

4. Implement _is_public_ipv4(addr: str) -> bool:
   - Parse with ipaddress.IPv4Address (catch ValueError, return False).
   - Return False for: is_private, is_loopback, is_multicast, is_link_local,
     is_reserved, is_unspecified. Return True only if none of these flags are set.
   - This satisfies DNS-02 (public IPv4 validation).

5. Implement resolve_public_ip() -> ipaddress.IPv4Address:
   - Query all 3 sources (use concurrent.futures.ThreadPoolExecutor max_workers=3
     with a 10-second total timeout so a hanging source never blocks indefinitely).
   - Collect non-None results from _fetch_ip.
   - Filter to only those passing _is_public_ipv4.
   - Count occurrences of each candidate IP string.
   - If any IP appears >= 2 times: return ipaddress.IPv4Address(winning_ip).
   - Otherwise: raise IPConsensusError("No 2-of-3 IP consensus achieved. DNS update aborted.")
   - The consensus check happens BEFORE the IPv4Address is returned, so callers
     always get a validated public IPv4 or an exception (fail closed — DNS-01).

Do NOT log inside this module. Logging is the caller's responsibility.
  </action>
  <verify>
    cd E:/ClaudeCode/VirtualFTP && python -c "
from src.wg_automate.dns.ip_resolver import resolve_public_ip, IPConsensusError, _is_public_ipv4
import ipaddress

# Validate private IP rejection (DNS-02)
assert not _is_public_ipv4('10.0.0.1')
assert not _is_public_ipv4('192.168.1.1')
assert not _is_public_ipv4('127.0.0.1')
assert not _is_public_ipv4('169.254.0.1')
assert not _is_public_ipv4('not-an-ip')

# Validate public IP acceptance
assert _is_public_ipv4('8.8.8.8')
assert _is_public_ipv4('1.1.1.1')

print('ip_resolver unit checks passed')
"
  </verify>
  <done>
    - _is_public_ipv4 rejects RFC 1918, loopback, multicast, link-local, and non-IP strings
    - _is_public_ipv4 accepts known public addresses
    - IPConsensusError is importable
    - resolve_public_ip is importable (live network call not required for this task)
    - src/wg_automate/dns/__init__.py exists and is importable
  </done>
</task>

<task type="auto">
  <name>Task 2: DuckDNS HTTPS updater with vault-sourced SecretBytes token</name>
  <files>
    src/wg_automate/dns/duckdns.py
  </files>
  <action>
Create src/wg_automate/dns/duckdns.py:

1. Define DuckDNSError(Exception) — raised on any update failure (bad response,
   network error, non-OK reply).

2. Implement update_dns(domain: str, token: "SecretBytes", ip: str) -> dict:
   - Import SecretBytes from security.secret_types (TYPE_CHECKING guard for the
     type hint; runtime isinstance check is fine).
   - Build the DuckDNS update URL:
     https://www.duckdns.org/update?domains={domain}&token={token_str}&ip={ip}&verbose=false
   - Extract the token string via token.decode() or str() — use the appropriate
     SecretBytes accessor. NEVER log or repr the token. NEVER pass it as a
     subprocess argument.
   - Use urllib.request.urlopen with ssl.create_default_context() for HTTPS with
     cert verification. Timeout: 10 seconds. No HTTP fallback.
   - Read and decode the response body; strip whitespace.
   - If response body is exactly "OK": success.
   - Otherwise: raise DuckDNSError(f"DuckDNS returned unexpected response (not 'OK')")
     — do NOT include the response body verbatim in the error message if it could
     contain token fragments. Safe to include a truncated prefix: body[:20].
   - Return a result dict:
     {
       "success": True/False,
       "domain": domain,
       "ip": ip,
       "timestamp": datetime.now(timezone.utc).isoformat(),
       "error": None or str(exception)
     }
   - Wrap the entire urllib call in try/except; on exception set success=False,
     error=str(exc), re-raise DuckDNSError only if you want the caller to abort.
     The return dict is always populated so the caller can pass it to audit.log().
   - DNS-03: token is SecretBytes parameter — never on disk, never in CLI args.
   - DNS-04: HTTPS only, cert verification via ssl.create_default_context(),
     response must be exactly "OK".
   - DNS-05: This function has no scheduling logic — the platform adapters
     (Phase 2) set up cron/launchd/Task Scheduler. This function is what those
     schedulers call.

3. Add module docstring citing DNS-03, DNS-04, DNS-05.

Update src/wg_automate/dns/__init__.py to export update_dns and DuckDNSError.
  </action>
  <verify>
    cd E:/ClaudeCode/VirtualFTP && python -c "
from src.wg_automate.dns.duckdns import update_dns, DuckDNSError
from src.wg_automate.dns import resolve_public_ip, IPConsensusError
from src.wg_automate.security.secret_types import SecretBytes
import inspect

# Verify SecretBytes type annotation in signature
sig = inspect.signature(update_dns)
params = list(sig.parameters.keys())
assert 'domain' in params
assert 'token' in params
assert 'ip' in params

# Verify DuckDNSError is an Exception subclass
assert issubclass(DuckDNSError, Exception)

# Verify __init__ exports work
from src.wg_automate.dns import update_dns as _ud, DuckDNSError as _de
print('duckdns module checks passed')
"
  </verify>
  <done>
    - update_dns(domain, token, ip) is importable and has correct signature
    - DuckDNSError is an Exception subclass
    - dns/__init__.py exports resolve_public_ip, IPConsensusError, update_dns, DuckDNSError
    - No HTTP (non-TLS) code path exists in duckdns.py
    - Token parameter is SecretBytes (never a plain str in the function signature)
  </done>
</task>

</tasks>

<verification>
1. All unit checks in task verify blocks pass.
2. python -c "import src.wg_automate.dns" completes without error.
3. grep -r "http://" src/wg_automate/dns/ returns no results (HTTPS-only enforced).
4. grep -r "requests" src/wg_automate/dns/ returns no results (stdlib urllib only).
5. grep -n "token" src/wg_automate/dns/duckdns.py confirms token is never logged
   (no logging.*, print, or repr calls with the token variable).
</verification>

<success_criteria>
- resolve_public_ip() queries 3 independent HTTPS sources concurrently (DNS-01)
- 2-of-3 consensus required; IPConsensusError raised if not achieved (DNS-01 fail closed)
- _is_public_ipv4 rejects private, loopback, multicast, link-local addresses (DNS-02)
- update_dns() accepts token as SecretBytes only — never plaintext (DNS-03)
- update_dns() uses HTTPS with ssl.create_default_context(); validates "OK" response (DNS-04)
- update_dns() is a callable function with no scheduling logic — DNS-05 is satisfied by the platform adapters that schedule this function
- All exports available from src.wg_automate.dns
</success_criteria>

<output>
After completion, create .planning/phases/03-dynamic-dns-and-audit/03-01-SUMMARY.md
following the summary template.
</output>

---
plan_id: "03-02"
phase: 03-dynamic-dns-and-audit
name: Append-only audit logging with platform-appropriate permissions
wave: 1
autonomous: true
objective: >
  Implement a tamper-evident append-only audit log that records every tool action
  with ISO 8601 UTC timestamps, enforces 640 permissions (Linux/macOS) or
  SYSTEM-only ACL (Windows), never writes secrets to log entries, and exposes
  get_recent_entries(n) for the Phase 4 audit-log CLI command.
files_modified:
  - src/wg_automate/security/audit.py
  - src/wg_automate/security/__init__.py
task_count: 2
requirement_ids: [AUDIT-01, AUDIT-02, AUDIT-03]
depends_on: []

must_haves:
  truths:
    - "Every log entry has an ISO 8601 UTC timestamp, action type string, and metadata dict"
    - "No SecretBytes value, passphrase, or token ever appears in a log entry"
    - "Log file has 640 permissions on Linux/macOS (root rw, group r)"
    - "Log file has SYSTEM-only ACL on Windows (via icacls, consistent with permissions.py pattern)"
    - "Appending to a missing log file creates it with correct permissions before first write"
    - "get_recent_entries(n=50) returns the last n log entries as a list of dicts"
    - "AuditLog is importable from security/ without side effects (no file I/O at import time)"
  artifacts:
    - path: "src/wg_automate/security/audit.py"
      provides: "AuditLog class with log(), get_recent_entries() methods; AuditEntry dataclass"
      exports: [AuditLog, AuditEntry, AuditError]
    - path: "src/wg_automate/security/__init__.py"
      provides: "Updated exports including AuditLog, AuditEntry, AuditError"
  key_links:
    - from: "src/wg_automate/security/audit.py"
      to: "src/wg_automate/security/permissions.py"
      via: "set_file_permissions(log_path, mode=0o640) for AUDIT-02"
      pattern: "set_file_permissions"
    - from: "src/wg_automate/security/audit.py"
      to: "log file on disk"
      via: "append-only open('a') with line-buffering, one JSON object per line"
      pattern: "open.*mode.*a"
---

<objective>
Build the append-only audit log that every other module writes to. This is the
tamper-evidence layer: once an entry is written, no API exists to modify or delete
it. The CLI command in Phase 4 reads entries via get_recent_entries().

Purpose: Every tool action must be traceable. AUDIT-01 requires timestamps and
metadata with no secret leakage. AUDIT-02 requires 640 perms. AUDIT-03 requires
a retrieval API the CLI will call.

Output:
- src/wg_automate/security/audit.py — AuditLog class, AuditEntry, AuditError
- Updated src/wg_automate/security/__init__.py
</objective>

<execution_context>
@C:/Users/mudig/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mudig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/ClaudeCode/VirtualFTP/.planning/PROJECT.md
@E:/ClaudeCode/VirtualFTP/.planning/ROADMAP.md
@E:/ClaudeCode/VirtualFTP/.planning/STATE.md

@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/permissions.py
@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/secret_types.py
@E:/ClaudeCode/VirtualFTP/src/wg_automate/security/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuditEntry dataclass and secret-scrubbing serializer</name>
  <files>
    src/wg_automate/security/audit.py
  </files>
  <action>
Create src/wg_automate/security/audit.py.

1. Define AuditError(Exception) — base error for audit log failures.

2. Define AuditEntry as a dataclass (from dataclasses import dataclass):
   Fields:
   - timestamp: str   — ISO 8601 UTC, e.g. "2026-03-20T14:23:01.123456+00:00"
   - action: str      — short action type string, e.g. "dns_update", "add_client"
   - metadata: dict   — arbitrary key/value context; secrets must be scrubbed before
                        this is populated (caller responsibility + _scrub_secrets guard)
   - success: bool    — True if the action succeeded
   - error: str | None — error message if success=False, else None

   Add a to_dict() method returning a plain dict (JSON-serializable).
   Add a classmethod from_dict(cls, d: dict) -> AuditEntry for deserializing
   stored log lines.

3. Implement _scrub_secrets(obj: object) -> object:
   - Recursively walk dicts and lists.
   - If a value is an instance of SecretBytes: replace it with "<redacted>".
   - If a value is a str and looks like a WireGuard private key (base64, 44 chars,
     ends with "="): replace with "<redacted-key>". Use:
     import re; _KEY_PATTERN = re.compile(r'^[A-Za-z0-9+/]{43}=$')
   - Return the scrubbed copy. Do NOT mutate the input.
   - This is AUDIT-01's secret-protection mechanism.

4. Add module docstring citing AUDIT-01, AUDIT-02, AUDIT-03.

Do NOT create the AuditLog class yet (that is Task 2). This task ends after
AuditEntry, AuditError, and _scrub_secrets are defined and importable.
  </action>
  <verify>
    cd E:/ClaudeCode/VirtualFTP && python -c "
from src.wg_automate.security.audit import AuditEntry, AuditError, _scrub_secrets
from src.wg_automate.security.secret_types import SecretBytes
import datetime

# AuditEntry round-trip
entry = AuditEntry(
    timestamp=datetime.datetime.now(datetime.timezone.utc).isoformat(),
    action='test_action',
    metadata={'ip': '1.2.3.4'},
    success=True,
    error=None,
)
d = entry.to_dict()
assert d['action'] == 'test_action'
restored = AuditEntry.from_dict(d)
assert restored.action == 'test_action'

# Secret scrubbing
secret = SecretBytes(b'secret-value')
scrubbed = _scrub_secrets({'token': secret, 'ip': '1.2.3.4'})
assert scrubbed['token'] == '<redacted>'
assert scrubbed['ip'] == '1.2.3.4'

# WireGuard key scrubbing (44-char base64 with trailing =)
key = 'wFpPkKzS8DGaKlGzfpn4Vhb7Kl/SqtJ8n5K1234567='
scrubbed2 = _scrub_secrets({'private_key': key})
assert scrubbed2['private_key'] == '<redacted-key>'

print('AuditEntry and _scrub_secrets checks passed')
"
  </verify>
  <done>
    - AuditEntry is a dataclass with timestamp, action, metadata, success, error fields
    - to_dict() and from_dict() work correctly (round-trip)
    - _scrub_secrets replaces SecretBytes with "&lt;redacted&gt;"
    - _scrub_secrets replaces 44-char base64 WireGuard keys with "&lt;redacted-key&gt;"
    - AuditError is an Exception subclass
  </done>
</task>

<task type="auto">
  <name>Task 2: AuditLog class with append-only writes, 640 permissions, and get_recent_entries</name>
  <files>
    src/wg_automate/security/audit.py
    src/wg_automate/security/__init__.py
  </files>
  <action>
Add AuditLog class to src/wg_automate/security/audit.py (below the existing
AuditEntry and _scrub_secrets definitions from Task 1):

1. AuditLog.__init__(self, log_path: Path):
   - Store log_path. Do NOT open the file or create the log at init time (no side
     effects on import or instantiation).

2. AuditLog._ensure_log_exists(self) -> None:
   - If the log file does not exist: create it (touch), then call
     set_file_permissions(self.log_path, mode=0o640) from security/permissions.py.
   - 0o640 = rw-r----- on Unix (AUDIT-02: root read/write, group read).
   - On Windows, set_file_permissions already routes to icacls with SYSTEM-only
     ACL — call it with mode=0o640 (the mode is ignored on Windows; the icacls
     path handles the actual restriction). This reuses the existing pattern from
     permissions.py without duplicating Windows-specific logic.
   - If log_path.parent does not exist, create it with parents=True.

3. AuditLog.log(self, action: str, metadata: dict, success: bool = True, error: str | None = None) -> AuditEntry:
   - Scrub metadata via _scrub_secrets(metadata).
   - Build AuditEntry with:
     timestamp = datetime.now(timezone.utc).isoformat()
     action = action
     metadata = scrubbed_metadata
     success = success
     error = error
   - Call _ensure_log_exists().
   - Open log_path in append mode ("a") with encoding="utf-8", newline="\n".
     Use line-buffering (buffering=1) so each entry flushes immediately.
   - Write json.dumps(entry.to_dict()) + "\n".
   - Return the AuditEntry.
   - AUDIT-01: ISO 8601 UTC timestamp, action type, metadata — all enforced here.
   - AUDIT-01: _scrub_secrets guarantees no secrets in log entries.

4. AuditLog.get_recent_entries(self, n: int = 50) -> list[AuditEntry]:
   - If log file does not exist: return [].
   - Read the file in binary mode, find the last n newline-terminated lines
     efficiently (read from end using seek if file is large, or simple
     readlines()[-n:] for correctness — pick whichever is simpler; the log will
     not be enormous in practice).
   - Parse each line as JSON via json.loads; construct AuditEntry via from_dict().
   - Skip malformed lines (bare except: continue) rather than raising.
   - Return list of AuditEntry objects, oldest first.
   - AUDIT-03: This is the method Phase 4's audit-log CLI command will call.

Update src/wg_automate/security/__init__.py:
- Add AuditLog, AuditEntry, AuditError to the exports (import from .audit).
  </action>
  <verify>
    cd E:/ClaudeCode/VirtualFTP && python -c "
import tempfile, os, json, sys
from pathlib import Path
from src.wg_automate.security.audit import AuditLog, AuditEntry, AuditError

# Write entries to a temp log
with tempfile.TemporaryDirectory() as tmp:
    log_path = Path(tmp) / 'audit.log'
    al = AuditLog(log_path)

    # File should not exist yet (no side effects on init)
    assert not log_path.exists(), 'Log file created at init (should not be)'

    # Log some entries
    e1 = al.log('dns_update', {'domain': 'test.duckdns.org', 'ip': '1.2.3.4'}, success=True)
    e2 = al.log('add_client', {'name': 'phone'}, success=True)
    e3 = al.log('remove_client', {'name': 'laptop'}, success=False, error='Not found')

    # File created
    assert log_path.exists()

    # Check permissions on Unix
    if sys.platform != 'win32':
        import stat
        mode = stat.S_IMODE(log_path.stat().st_mode)
        assert mode == 0o640, f'Expected 0o640, got {oct(mode)}'

    # get_recent_entries round-trip
    entries = al.get_recent_entries(50)
    assert len(entries) == 3
    assert entries[0].action == 'dns_update'
    assert entries[2].success == False
    assert entries[2].error == 'Not found'

    # get_recent_entries with limit
    last1 = al.get_recent_entries(1)
    assert len(last1) == 1
    assert last1[0].action == 'remove_client'

    # Verify no secrets leaked into log
    raw = log_path.read_text()
    assert 'SecretBytes' not in raw
    assert '<redacted>' not in raw  # metadata had no secrets in this test

    print('AuditLog full integration checks passed')
"
  </verify>
  <done>
    - AuditLog has no side effects at instantiation (no file I/O)
    - First call to log() creates the log file with correct permissions (640 Unix)
    - log() writes one JSON line per entry with ISO 8601 UTC timestamp
    - get_recent_entries(n) returns last n entries as AuditEntry list, oldest first
    - get_recent_entries() on missing file returns [] (no exception)
    - AuditLog, AuditEntry, AuditError are exportable from src.wg_automate.security
  </done>
</task>

</tasks>

<verification>
1. All task verify blocks pass.
2. python -c "from src.wg_automate.security import AuditLog, AuditEntry, AuditError; print('exports ok')" succeeds.
3. grep -n "SecretBytes" src/wg_automate/security/audit.py confirms SecretBytes is only imported for isinstance check and _scrub_secrets — never used as a field type that could be serialized.
4. grep -n "0o640" src/wg_automate/security/audit.py confirms the correct permission mode.
5. grep -n "open(" src/wg_automate/security/audit.py confirms no open() calls outside of log() and get_recent_entries() (no init-time file I/O).
</verification>

<success_criteria>
- Every log entry has ISO 8601 UTC timestamp, action, metadata, success, error (AUDIT-01)
- _scrub_secrets ensures no SecretBytes or WireGuard private keys appear in entries (AUDIT-01)
- Log file permissions: 640 on Linux/macOS, SYSTEM-only via icacls on Windows (AUDIT-02)
- get_recent_entries(n=50) returns the last n entries as AuditEntry list (AUDIT-03)
- No file I/O at import or instantiation (safe to import in any context)
- AuditLog, AuditEntry, AuditError all exported from src.wg_automate.security
</success_criteria>

<output>
After completion, create .planning/phases/03-dynamic-dns-and-audit/03-02-SUMMARY.md
following the summary template.
</output>

# Direct dependencies for wg-automate (Python >=3.12,<3.15)
# Compile with: pip-compile --generate-hashes requirements.in  (HARD-02)
# Audit with: pip-audit  (HARD-03)

cryptography>=46.0,<47
argon2-cffi>=25.1,<26
jinja2>=3.1.6,<4
filelock>=3.20.3,<4
click>=8.3.1,<9


  # User management dashboard

  This is a code bundle for User management dashboard. The original project is available at https://www.figma.com/design/lzkJCGw4ddvK75gvCq7jOU/User-management-dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  